port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 50100
  tcp_port: 7881
  use_external_ip: false
  node_ip: 100.x.x.x  # Your Tailscale IP (run: tailscale ip -4)

# Must match .env LIVEKIT_API_KEY and LIVEKIT_API_SECRET
keys:
  devkey: secret

logging:
  level: info